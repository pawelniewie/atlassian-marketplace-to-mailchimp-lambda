{
  "description": "Java example function with existing pom.xml",
  "handler": "lambda.Example::handler",
  "runtime": "java",
  "timeout": 60,
  "hooks": {
    "build": "mvn package",
    "clean": "mvn clean"
  }
}